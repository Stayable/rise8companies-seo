# rise8companies.com — the static scaffold needs no secrets.
# Add env vars here (documented, no real values) if/when we wire:
#   - a contact-form submission endpoint
#   - any external content source
