Tiny hairline-bordered pill for metadata (location corridors, markets, status flags). Square, uppercase, 9px Inter.

```jsx
<div style={{ display: 'flex', gap: 10 }}>
  <Tag>I-4 corridor</Tag>
  <Tag>Renovated 2024</Tag>
</div>
```

No fill and no radius by default — it reads as a label, not a button.
